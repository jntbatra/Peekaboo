{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the Settings window — minimal permissions only",
  "windows": [
    "settings"
  ],
  "permissions": [
    "core:default"
  ]
}
